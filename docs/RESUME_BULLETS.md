# Resume Bullets

Choose the version that best fits the target role; these bullets describe
verified project work without unmeasured scale claims.

- Built a versioned Kafka-to-PostgreSQL e-commerce ingestion pipeline with
  Pydantic validation, transactional domain writes, at-least-once delivery,
  and `event_id`-based idempotent replay handling.
- Orchestrated Airflow and dbt transformations from operational PostgreSQL
  tables into tested staging, intermediate, and dimensional analytics marts.
- Delivered a read-only FastAPI analytics API and React/TypeScript dashboard
  for revenue, orders, payments, product performance, and fulfillment metrics.
- Added Prometheus/Grafana observability and Terraform/AWS deployment with
  ECS/Fargate, private RDS, ECR, S3/CloudFront, CloudWatch, and GitHub OIDC
  short-lived credentials.
